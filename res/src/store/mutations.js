/**
 * Popups
 * @param state
 * @param popups
 */
export const popups = (state, popups) => {
    state.popups = popups
}

/**
 * Site Setup
 * @param state
 * @param setup
 */
export const siteSetup = (state, setup) => {
    state.siteSetup = setup
}

/**
 * CPT Setup
 * @param state
 * @param setup
 */
export const cptSetup = (state, setup) => {
    state.cptSetup = setup
}

/**
 * Post Exclusion Setup
 * @since 1.1.1
 */
export const postsExclusionSetup = (state, setup) => {
    state.postsExclusionSetup = setup
}

/**
 * Posts Setup
 * @param state
 * @param setup
 */
export const postsSetup = (state, setup) => {
    state.postsSetup = setup
}
